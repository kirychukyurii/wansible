# webitel_frontend

Installs the Webitel browser-based front-end applications from the Webitel S3 APT repository. The packages ship static assets served by NGINX; no additional configuration is required.

See `defaults/main.yml` for the variable and the default package list.
